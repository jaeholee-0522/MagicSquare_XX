# Magic Square 4×4 — QA Control/Boundary 코드 스멜 점검 보고서

## 1. 문서 목적

본 보고서는 REFACTOR phase 착수 후 **quality-assurance-engineer** 에이전트가 수행한  
`src/control/*`, `src/boundary/*` (Screen UI 포함) 정적 코드 품질 점검 결과를 정리한다.  
점검 산출물은 README §P2 QA 체크리스트(RF-P2-H/M/L)로 반영되었다.

| Field | Value |
|---|---|
| Report ID | Report/15 |
| Branch | `refactor/refactor` |
| Prior Session | `Report/14` — REFACTOR P0 ECB·QA·ECB 매핑 |
| Agent | `quality-assurance-engineer` (read-only) |
| Session Date | 2026-05-29 |
| TDD Phase | **REFACTOR** (P0 완료, P2-H 미착수) |
| Final Status | **정적 리뷰 완료** — High 4건 · Medium 7건 · Low 2건 |

---

## 2. 작업 개요

| 단계 | 작업 | 산출물 | 결과 |
|---|---|---|---|
| QA 점검 | Control/Boundary 11개 Python 파일 전수 검토 | 코드 스멜 표 | High 4 · Medium 7 · Low 2 |
| README 반영 | P2-H/M/L 체크리스트·요약표 | `README.md` §P2 QA | RF-P2-H01~H04 등 13항 [ ] |
| GitHub | magic20 push | `main`, `develop`, `refactor/refactor` | `0c28e21`~ |
| Export | Report·Transcript | 본 보고서, `Prompt/15.*` | 본 Turn |

---

## 3. 점검 범위

### 3.1 대상 파일

| ECB Layer | 경로 |
|---|---|
| Control | `src/control/solve_use_case.py`, `ports.py`, `__init__.py` |
| Boundary (core) | `src/boundary/boundary_validator.py`, `contracts.py`, `__init__.py` |
| Boundary (Screen) | `src/boundary/screen/*.py` (app, main_window, grid_input_widget, solve_presenter, sample_puzzles) |

### 3.2 참조 전용 (결합·중복만 기록)

| 구 경로 | 현재 대응 | 비고 |
|---|---|---|
| `input_validator.py` | `boundary_validator.py` | 미존재 — 대체 완료 |
| `two_cell_solver.py` | `entity/solver.py` `TwoCellSolver` | int[6] 형식 Boundary와 중복 결합 |

### 3.3 계약 기준

| Prompt E-code | 구현 코드 | Boundary 위치 |
|---|---|---|
| E001 size / E003 null | `INVALID_SIZE` | `_is_size_invalid` |
| E002 blank count | `INVALID_BLANK_COUNT` | `_is_blank_count_invalid` |
| E004 range | `INVALID_RANGE` | `_is_range_invalid` |
| E005 duplicate | `INVALID_DUPLICATE` | `_is_duplicate_invalid` |
| E006/E007 | **미정의** | — |
| Success | bare `list[int]` len=6 | Entity→Control→Presenter (**Boundary 검증 없음**) |

SSOT: `src/contracts/grid_constants.py` — `GRID_SIZE=4`, `MIN/MAX=1/16`, `MAGIC=34`, `REQUIRED_BLANK_COUNT=2`

---

## 4. 점검 항목별 결과

| 점검 항목 | 결과 |
|---|---|
| 이해하기 힘든 이름 | Low 1건 (`SolveResult` type alias) |
| 중복 코드 | Medium 3건 |
| 긴 함수 (>20줄) | Medium 3건 |
| 하드코딩 매직 넘버 | Medium 2건 + Low 4건 |
| 긴 매개변수 목록 (>3) | **해당 없음** |
| 불필요한 주석 | **해당 없음** |
| 사용하지 않는 변수·함수 | Low 2건 |
| 예외를 제어 흐름으로 오용 | **해당 없음** |
| 무시된 반환값 / Dead code | High 4건 + Low 1건 |
| ECB 레이어 책임 혼재 | High 1건 + Medium 3건 |

---

## 5. Code Smell Table (전체)

| 파일명 | 줄번호 | 스멜 종류 | 문제 설명 | 우선순위 |
|---|---|---|---|---|
| `src/boundary/screen/solve_presenter.py` | 71–74 | Ignored return values / ECB 책임 | PRD `ResultFormatter`·U-OUT-01~03에 해당하는 **int[6] 스키마 검증 없이** `list(result)`를 `SuccessOutcome.placement`로 통과 | **High** |
| `src/boundary/screen/grid_input_widget.py` | 121–126 | ECB 계약 / Ignored return values | `apply_solution`이 int[6] 길이·1-index 범위 미검증 → malformed placement 시 **IndexError** | **High** |
| `src/boundary/screen/grid_input_widget.py` | 117–119 | ECB 계약 | `highlight_placements`도 좌표 검증 없음 | **High** |
| `src/boundary/screen/grid_input_widget.py` | 83–88 | ECB 계약 | `set_grid`가 `GRID_SIZE` 검증 없이 인덱싱 → 비4×4 **IndexError** | **High** |
| `src/boundary/screen/main_window.py` | 135–136 | Duplicate code / ECB 계약 | int[6] 언팩·`apply_solution` 호출 전 스키마 검증 없음; 3중 소비 | **High** |
| `src/control/solve_use_case.py` | 23, 32 | ECB 계약 | `execute(grid: Any)` — 입력 `int[][]` 계약 타입 소실 | **High** |
| `src/boundary/boundary_validator.py` | 57–86 | Duplicate code | range/blank/duplicate 검증이 각각 full-grid 이중 순회 | Medium |
| `src/boundary/screen/main_window.py` | 63 | Hardcoded magic numbers | 힌트 `"1–16"` — `MIN_VALUE`/`MAX_VALUE` SSOT 미사용 | Medium |
| `src/boundary/screen/main_window.py` | 47–100 | Long functions | `_build_ui` 본문 52줄 (>20) | Medium |
| `src/boundary/screen/main_window.py` | 148–177 | Long functions | `_show_error` 본문 30줄 (>20) | Medium |
| `src/boundary/screen/grid_input_widget.py` | 34–67 | Long functions | `_build_ui` 본문 34줄 (>20) | Medium |
| `src/boundary/screen/*.py` | 117–126 등 | Duplicate code (참조 결합) | int[6] 언팩·1-index 변환 3곳; `entity/solver.py` `_try_placement`와 형식 중복 | Medium |
| `src/boundary/screen/main_window.py` | 18, 36 | ECB layer mixing | Screen→`bootstrap` 직접 의존; `app.py`와 이중 composition root | Medium |
| `src/boundary/contracts.py` | 1–29 | Duplicate code | `contracts.validation_errors` re-export shim — 이중 import 경로 | Medium |
| `src/control/ports.py` | 10–17 | ECB layer mixing | `InputValidator`만 port; `DomainResolver` port는 entity 패키지 | Medium |
| `src/contracts/validation_errors.py` | 10–19 | Hardcoded magic numbers (참조) | 메시지 `"4x4"`, `"2"`, `"1-16"` ↔ `grid_constants` SSOT 분리 | Medium |
| `entity/solver.py` (참조) | 10, 65–72 | Unused / 참조 coupling | `SOLUTION_LENGTH=6` 미사용; int[6] SSOT Boundary 미공유 | Medium |
| `src/boundary/screen/grid_input_widget.py` | 20–22, 103–116 | Dead code | `_ERROR_STYLE`, `is_error=True` 호출처 없음 | Low |
| `src/boundary/screen/app.py` | 20 | Hardcoded magic numbers | UI 표시 `"Magic Square 4x4"` | Low |
| `src/control/ports.py` | 10 | Hard-to-understand names | `SolveResult` union type alias 혼동 | Low |

---

## 6. 회귀 버그 후보 (정적)

| ID | 설명 | 재현 조건 | 운영 영향 |
|---|---|---|---|
| B-01 | `set_grid` 비4×4 → IndexError | `GridInputWidget.set_grid([[1,2],[3,4]])` | 샘플·외부 입력 경로 |
| B-02 | malformed placement → 예외 | Presenter가 len≠6 list를 Success로 매핑 | REFACTOR 회귀 |
| B-03 | 좌표 0 또는 5+ → IndexError | 0-index 좌표 혼입 | REFACTOR 회귀 |

> Happy path에서는 Domain이 올바른 int[6]를 반환하므로 **운영 중 미발현**; REFACTOR 회귀 위험으로 분류.

---

## 7. 양호 항목 (Passed)

- FR-01 검증 순서 **SIZE → RANGE → BLANK COUNT → DUPLICATE** (`boundary_validator.py`)
- `BoundaryValidator`가 `grid_constants` SSOT 사용 (4/16/2 하드코딩 없음)
- `SolveUseCase`가 검증 실패 시 Domain 미호출 (BR-05)
- ECB import 방향: Boundary→Control, Control→Entity (`test_architecture_imports.py` 일치)
- `ValidationErrorResponse` / `DomainErrorResponse` pydantic frozen 모델 유지

---

## 8. REFACTOR 권장 순서

1. **P2-H:** `ResultFormatter` — Presenter 성공 분기 int[6] 검증 (U-OUT Boundary enforcement)
2. **P2-H:** `GridInputWidget` set_grid / apply_solution / highlight_placements 가드
3. **P2-H:** `SolveUseCase.execute` — `grid: Any` → `list[list[int]]`
4. **P2-M:** int[6] helper SSOT (`SOLUTION_LENGTH=6` 공유)
5. **P2-M:** BoundaryValidator 단일 순회, composition root 정리, 장함수 분리
6. **P1 선행 권장:** RF-P1-01/02/05 — Control mock·Boundary 비정수·GUI headless

---

## 9. README 체크리스트 연동

| README Task | 본 Report High/Medium 매핑 |
|---|---|
| RF-P2-H01 | solve_presenter int[6] 미검증 |
| RF-P2-H02 | set_grid GRID_SIZE 미검증 |
| RF-P2-H03 | apply_solution / highlight_placements 가드 |
| RF-P2-H04 | execute(grid: Any) |
| RF-P2-M01~M07 | 중복·SSOT·장함수·composition root |
| RF-P2-L01~L02 | Dead code·type alias |
| RF-ECB-P1-06 | int[6]·GridInputWidget 스키마 가드 (P1 연동) |

---

## 10. Git 커밋 이력 (관련)

```text
0c28e21 docs: add QA code smell checklist for Control/Boundary REFACTOR phase.
3045e2a refactor: ECB contracts layer, bootstrap wiring, and REFACTOR README checklist.
```

**원격:** `magic20` → `https://github.com/jaeholee-0522/MagicSquare_20.git`

---

## 11. Out of Scope (본 세션)

| 항목 | 상태 |
|---|---|
| RF-P2-H01~H04 코드 구현 | 미착수 — README [ ] only |
| RF-P1 테스트 보강 | 미착수 |
| Entity layer 점검 | 대상 외 (참조 coupling만 기록) |
| E006/E007 정의 | 코드베이스·PRD 미정의 |

---

## 12. 결론

- Control/Boundary ECB 레이어 **정적 QA 점검**을 완료하고 High 4건(계약·int[6]·UI IndexError)을 식별했다.
- 점검 결과를 README §P2 QA(RF-P2-H/M/L)로 고정해 REFACTOR 배치 SSOT로 사용한다.
- **1순위 후속:** RF-P2-H01~H04 (ResultFormatter + UI 가드) — Golden Master·U-OUT 계약 불변 전제.
- 본 Turn 산출물: **Report/15**, **Prompt/15** Export.
